import { TextareaAutogrowDirective } from './textarea-autogrow.directive';

describe('TextareaAutogrowDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaAutogrowDirective();
    expect(directive).toBeTruthy();
  });
});
